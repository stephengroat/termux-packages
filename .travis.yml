language: generic
sudo: required
services:
  - docker
script:
  - mkdir debs && chmod 777 debs
  - git diff --name-only HEAD...master
  - ./scripts/run-docker.sh ./build-package.sh libandroid-support
